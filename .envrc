#!/usr/bin/env bash

if [ -f ".envrc.local" ]; then
  source .envrc.local
  echo ".envrc.local sourced."
elif type nix-shell >/dev/null 2>&1; then
    use flake
else
    echo "No .envrc.local and no nix-shell found, please install Nix or run the environment manually."
fi

# vim: syntax=bash
